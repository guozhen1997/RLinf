env_type: polaris

total_num_envs: null

auto_reset: False
ignore_terminations: False
use_rel_reward: False
seed: 0
group_size: 1

reward_coef: 1.0
use_fixed_reset_state_ids: False
max_steps_per_rollout_epoch: 250
max_episode_steps: 250 # max episode steps for truncation

video_cfg:
  save_video: False
  info_on_video: True
  fps: 10
  video_base_dir: ${runner.logger.log_path}/video/train

init_params:
  id: "DROID-TapeIntoContainer"
  num_envs: null
  max_episode_steps: ${env.train.max_episode_steps}
  task_description: "Put the tape into the container"
  usd_file: "/mnt/public/lizj/docker_data/Codespace/RLinf/dataset/PolaRiS-Hub/tape_into_container/scene.usda"
  dataset_path: "/mnt/public/lizj/docker_data/Codespace/RLinf/dataset/PolaRiS-Hub"
  wrist_cam:
    height: 224
    width: 224
  table_cam:
    height: 224
    width: 224
